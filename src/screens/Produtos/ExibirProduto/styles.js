import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    input: {
        height: 50, 
        marginTop:7
    },
    action: {
        width: 90, 
        marginLeft: 20, 
        marginTop:20
    }
})

export default styles